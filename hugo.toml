baseURL = 'https://codvatech-labs.github.io/ctl-devops-bootcamp'
languageCode = 'en-us'
title = 'My New Hugo Site'

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-relearn"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]
