baseURL = 'https://codvatech-labs.github.io/'
languageCode = 'en-us'
title = 'CodvaTech Labs DevOps BootCamp'

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-relearn-main"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "JSON"]
